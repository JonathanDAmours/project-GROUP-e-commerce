import { useEffect, useState, useRef } from "react";

// this will stick our  navbar to the top on scroll

import React from "react";

export default function useSticky() {
  const [sticky, setSticky] = useState(false);

  const handleScroll = () => {};
}
